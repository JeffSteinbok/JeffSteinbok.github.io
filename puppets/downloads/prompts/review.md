@copilot please review this pull request before it is merged, and address anything you find
by pushing follow-up commits to this branch.

Check that:

- The change fully resolves the linked issue and stays within its scope.
- The code follows the conventions and structure of the surrounding codebase.
- Tests cover the new behavior, and the build, tests, and linters all pass.
- No dead code, debugging output, secrets, or unrelated changes are left behind.
- Edge cases and error paths are handled sensibly.

If everything looks good, say so briefly. If not, fix it and push.
